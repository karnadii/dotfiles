<!-- context7 -->
Use the `context7` MCP tool to fetch documentation. It is the preferred method and available as a tool in this environment. If the MCP tool is unavailable or returns an error, fall back to the `ctx7` CLI:

1. Resolve library: `npx ctx7@latest library <name> "<user's question>"`
2. Fetch docs: `npx ctx7@latest docs <libraryId> "<user's question>"`

If the MCP or CLI fail with a quota error, suggest the user set `CONTEXT7_API_KEY` env var for higher limits. Do not silently fall back to training data.
<!-- context7 -->

<!-- zsh-coding-tools -->
Always use the `skill` tool to load the `zsh-coding-tools` skill at the start of any coding task. It documents the high-performance CLI tools (`fd`, `rg`, `eza`, `bat`, `fzf`, `zoxide`) available in Karnadi's shell environment. Use these tools instead of slower alternatives: use `rg` instead of `grep`, `fd` instead of `find`, `eza` instead of `ls`, `bat` instead of `cat`.
<!-- zsh-coding-tools -->

<!-- git-commit -->
# Git Commit & Authorship Guidelines
We utilize a multi-agent workflow powered by various open-source models. Because code is continuously generated, modified, and reviewed by multiple different agents, attempting to track which specific model wrote which line of code is inaccurate and pollutes our git history.

Therefore, you MUST NOT include any AI attribution, footers, or watermarks in your commit messages.
- OMIT all "Co-authored-by:" tags.
- OMIT all "Generated by" or tool-specific signatures.
- Output ONLY the standard commit subject and body.
<!-- git-commit -->
