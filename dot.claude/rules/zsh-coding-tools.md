Use the high-performance CLI tools available in this shell environment for code search, file finding, and navigation:
- `rg` (ripgrep) instead of `grep` for content search
- `fd` instead of `find` for file finding
- `eza` instead of `ls` for directory listing
- `bat` instead of `cat` for file viewing (syntax highlighting, git diff)
